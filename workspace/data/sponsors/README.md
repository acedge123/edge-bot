# Local sponsors database

- **`sponsors.json`** — working file (gitignored). Copy from **`sponsors.schema.example.json`** to start.
- Enrichment uses **`GOOGLE_MAPS_API_KEY`** and the **`google-places`** / **`sponsors-database`** skills under `workspace/skills/`.

Do not commit real `sponsors.json` if it contains sensitive or contractual data unless policy allows.
